import RioSocialMedia from "@/src/components/RioSocialMedia";

/**
 * Parse url
 * @param   {String} url URLs map to use
 * @param   {String} size URL name to parse
 */

export default RioSocialMedia;
